dbt_train:
  outputs:
    dev:
      type: postgres
      threads: 1
      host: localhost
      port: 5432
      user: "{{ env('DBT_USER') }}"
      pass: "{{ env('DBT_PASS') }}"
      dbname: dbt_test
      schema: src

  target: dev
